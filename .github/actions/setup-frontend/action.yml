name: Setup frontend
description: Install the frontend dependencies with npm caching.

runs:
  using: composite
  steps:
    - uses: actions/setup-node@v6
      with:
        node-version: 20
        cache: npm
        cache-dependency-path: frontend/package-lock.json

    - name: Install frontend dependencies
      shell: bash
      run: npm ci --prefix frontend
